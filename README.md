# mishan

`mishan` is a library that adds AI-agent capabilities to command-line programs.

It is inspired by [kubectl-ai](https://github.com/GoogleCloudPlatform/kubectl-ai) and started from a fork of that project.  Besides `kubectl`, every command-line program needs an AI agent.  With `mishan`, developers can transform their applications to AI agents with a few lines of code.

